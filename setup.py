#!/usr/bin/python
from setuptools import setup, find_packages

setup(
    name='ring-check',
    version='0.1',
    description='Swift Ring Checker',
    author='SwiftStack',
    packages=find_packages(),
    entry_points={
        'console_scripts': [],
    },
)
